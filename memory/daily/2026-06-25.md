# 2026-06-25 每日笔记

## 主题:交易思想梳理整合 (trading-philosophy) + v2 service 层清理

经 grill-me 锁定双引擎交易体系,产出权威规格 `docs/standards/trading-philosophy.md`,
并据此做 Phase 2 代码整合;过程中暴露并清理了 v2-rewrite 遗留的大量 service/test 破损。

### Phase 1:trading-philosophy 规格 (grill 决策)

- **北极星 = 双引擎**:价值复利引擎 (ai-berkshire 四大师) + 产业链卡点引擎 (serenity),不互相裁决
- **hybrid 汇合**:serenity 选股 (WHICH) + ai-berkshire 估值/红线 (PRICE+RISK),汇入一张草稿
- **评分 profile**:按 source 选权重 (quality_screen→复利 / theme_scan→主题:李录降权+卡点维度)
- **去重规则×3**:① 持久优势三镜 (卡点/护城河/好生意) 同源整师折叠封顶 ② 证据分级两层 (条目级 strong/med/weak/lead + 包级 A/B/C) ③ 失败机制合并 (serenity 失败条件并入芒格 failure_scenarios)
- 文档放 `docs/standards/`(不是 `docs/reference/` —— 后者被 gitignore)

### Phase 2:实现 (全程 TDD,commit 825cec0 + 后续)

- **评分核心 hybrid**:LLM 算分降级为 advisory,Python 按 PROFILE_WEIGHTS 复核权威分 + 同源封顶 (`scoring_config.py` + `scoring.py` + deep_research 接入 + advantage_source 枚举)
- **serenity theme_scan 引擎**:`ThemeScanReport` 模型 + migration + 5 步 schemas/prompts + `theme_scan_pipeline.run(theme)` (A 股代码主表校验 + 卡点排序 + empty 短路) + `POST /api/theme-scan`;scarcity_score 复用注入 deep_research theme profile
- **§4.3 失败合并** (commit 9971f69):`deep_research.run(failure_conditions=)` 仅注入芒格 prompt

### 历史遗留清理 (commit 299792f / 7d86927 / 0e49fb8)

- **测试**:删 56 个 v1 死测试 (collection error) + #15 又删 ~5 个 + 改写 ~25 个 stale 测试 → 全套 **552 passed, 0 failed** (自 v2-rewrite 后首次全绿)
- **Alembic 断根修复**:原 base 迁移被删致空库 upgrade 失败 → 压缩 52 条破损链为单一基线 `v2_baseline_squash`(从模型 create_all);dev DB 用 `alembic stamp v2_baseline_squash --purge` 对齐
- **删 3 孤立死服务**:cockpit_service (孤立 v1,router 已是 Phase-3 stub) + cashflow_service + market_temperature_service
- **修 5 个 live 生产 bug**:trade_service 卖出崩溃 (少下划线) / holding_service portfolio (cashflow_goal→CashBalance) / universe_service (candidate+watchlist 模型→StockLifecycle 状态) / notification_service (重构 in-app-only) / cockpit 路径

### §7 Draft dual-thesis — 暂缓

调查发现:v2 无 BUY-draft 生成流程 (`draft_service.emit` 是无调用方的 stub,deep_research→draft 未接线,显式推迟 Phase 5 draft_generator)。现在加 Draft 字段=死字段反模式。**§7 绑 Phase-5 draft_generator,届时一并做**。

### 决策依据来源切换

注意:本会话用 `docs/standards/trading-philosophy.md` 作为交易体系权威。它**取代**了 invest{1,2,3} 的散落约定,与 `redesign-decisions-v2.md` 互补 (后者是工程决策,前者是交易思想)。
