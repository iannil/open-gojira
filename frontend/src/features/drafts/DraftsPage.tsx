import { Result } from 'antd';

/**
 * Drafts — v2 stub.
 *
 * v1 DraftsPage 已移除（依赖 DisciplineChecklistModal）。
 * Phase 3 将重建为 1-click inline 审批。
 */
export default function DraftsPage() {
  return (
    <Result
      status="info"
      title="Drafts — v2 待重建"
      subTitle="v1 DraftsPage 已移除。Phase 3 将重建为 1-click inline 审批流。"
    />
  );
}
